export enum ControlBlocksArgs {
    CONDITION = 'CONDITION',
    SUBSTACK = 'SUBSTACK',
    SUBSTACK2 = 'SUBSTACK2',
}

export enum SensingBlockArgs {
    KEY_OPTION = 'KEY_OPTION',
}

export enum DataBlockArgs {
    VARIABLE = 'VARIABLE',
    VALUE = 'VALUE',
}

export enum MotionBlockArgs {
    X = 'X',
    Y = 'Y',
}
